# Copy to ./local-settings.properties (ignored by Git), then supply local values.
# Existing login endpoint reads accounts from legacy.auth.users.<username>.
# legacy.auth.users.your-user=REPLACE_LOCALLY
external.nrt.api.url=${NRT_API_URL:}
external.nrt.api.key=${NRT_API_KEY:}
