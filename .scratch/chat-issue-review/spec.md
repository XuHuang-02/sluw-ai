# 原聊天中的问题件提交试判：需求重新对齐

Status: ready-for-human

更新时间：2026-09-14。正在重新对齐选路实验需求；下述历史实现不代表本轮最终范围。

## 已明确的范围

- 用户要求移除之前新增的模块一、二，重新理解需求。
- 基于原 sluw-ai 对话功能进行小范围试验。
- 用户手动提供业务信息；目前不完整嵌入业务、不自动查询业务数据库或执行核保/下发操作。
- 业务参考是用户本机 GBK 编码 AutoSendBL.java，第137行的问题件提交部分（dealIssue）。
- 此前独立模块已删除；本轮只在原聊天工程新增试判逻辑。

## 代码事实与待区分概念

dealIssue 读取已有核保错误、历史下发、任务和记事本记录，依次选择退出自动处理、核保通过调用、内部问题件、外1及其他后续处理。它本身没有独立判断客户回复或补件是否实质解决问题。

用户已明确选择：根据已有核保记录，建议下一步转人工、继续下发问题件等处理去向；不判断补件内容是否实质解决原问题。

## 已确认的判断与输入约定

- 严格遵循 dealIssue 及依赖方法的实际条件、执行顺序。RAGFlow仅解释规则话术，不改变流程去向。
- 代码未明确下一步时如实说明，不把所有 return 解释为转人工，也不把调用通过方法等同于业务实际通过成功。
- 提供固定JSON模板，保留业务字段名；当前与历史核保记录分开，用户手动填写并粘贴到原聊天框。
- 缺失资料不能当作否定条件；未提供历史不等于从未下发。信息不足时说明阻塞条件并追问，不猜测去向。
- 具体业务样例及字段完整性尚未取得，不宣称具备完整输入数据。

## 已确认的回复、会话与验收约定

- 回复固定包含：处理去向、命中的代码条件、引用的输入记录、缺失信息。
- 每次提交完整JSON，独立判断这一单；补充资料时修改原JSON并重新发送。
- 聊天历史可用于解释，不作为本次判断的业务事实，不合并历史消息中的字段。
- 准备按代码人工确认预期去向的案例，覆盖退出自动处理、人工处理、建议通过、内部问题件、外1下发次数边界、体检/契调及资料缺失。
- 对照去向一致性、代码依据正确性、输入引用正确性，以及缺失时是否停止猜测。合成案例不证明真实业务准确率。

## 整体范围待最后核对

现有对话入口与模型承担本次流程试判；判断流程依据使用固定的业务代码说明，RAGFlow只提供解释。不要重新引入独立规则引擎代替模型作出判断后将结果伪装成AI能力。

本次交付范围：固定JSON模板、对话试判逻辑、固定回复结构，以及有预期去向的测试案例。不新增独立系统、规则发布、业务接口、自动数据库查询、正式报告或业务执行动作。

用户已确认在原聊天页面增加“问题件提交试判”开关。

用户已完成整体确认。已实施入口、提示词、完整JSON模板与16个对照案例；7个自动化工程测试通过。真实模型的分支准确性及案例业务含义仍待试用核对。

## Comments

2026-09-11：按用户要求删除独立模块；启动新一轮需求访谈。原聊天源码未变动。

2026-09-11：用户明确选择流程去向判断。继续确认流程依据、输入方式和缺失信息处理；未修改聊天代码。

2026-09-11：用户确认严格遵循代码，RAGFlow仅解释；固定JSON手动粘贴、字段名保留、当前历史分开；缺失历史不能推定为未下发。

2026-09-11：用户确认四项回复结构、完整JSON单次独立判断、历史仅用于解释，并确认按代码预期去向准备覆盖分支及缺失场景的测试案例。

2026-09-11：用户确认整体范围后实施。原工程编译通过；7个测试通过；前端脚本语法检查通过。未调用真实模型或业务服务，不能宣称16个案例已获AI正确判断。


## 2026-09-11：局部实验收窄

用户要求按纠正后的思路修改代码，先在一个部分实验并给出直接操作方式。选择sendnbwtj调用前的内部/机构下发准备，明确入口为人工已确认的实验前提，不重新验证上游筛选。

新增experimentStage=BEFORE_SEND_INTERNAL_AGENCY、entryConfirmed、candidateErrors和lcissuepol输入；专用提示词只分析sendnbwtj及dealNoticedata。解释伴生条件、内部内容与机构内容来源、拟更新范围；不改变类型和规则，不执行动作。原完整去向试判兼容保留。

11个工程测试通过；新增8个对照场景和一键填入合成示例。模型替身仅验证接线，不证明真实模型判断准确。操作文档：docs/trial/send-preparation.md。


## 2026-09-14：试判入口交互修复

修复局部函数被 HTML 内联 onclick 调用导致的 ReferenceError，改为独立脚本中绑定按钮事件；试判开关明确显示。新增工具按钮沿用蓝灰色圆角样式，提供读取中、成功、失败与模式切换提示，读取失败保留原输入。

使用隔离浏览器页面与合成接口复现旧错误，修复后验证六项：示例填入并启用试判、切回普通问答、取消覆盖、读取失败保留内容、窄屏布局、外部 CDN 不可用时示例填入。未调用真实模型或登录服务。


## 2026-09-14：重新对齐 AI 选路职责（优先于此前实验范围）

已确认：
- AI 根据已有核保记录和明确提示词选择处理去向，不判断客户补件是否实质解决问题。
- 首版严格保持 AutoSendBL 的分支条件、顺序和例外；原代码作为判断依据及案例预期参照。
- RAGFlow 可解释或补充规则依据，不得改变分支优先级或推翻明确代码条件。
- 下发与流转由未来接口对应的服务执行；当前实验手动输入、仅返回建议，不执行业务操作。
- 输出采用固定去向选项，并包含命中条件、引用输入记录和缺失信息。
- 明确区分退出自动处理与信息不足、无法确定；不将所有 return 解释为转人工。
- 输入按选路需要精简，执行服务所需信息不自动成为模型必需输入。

待确认：
- 已确认首版覆盖 dealIssue 全部选路分支及必要辅助判断，不包含 dealTransUW。
- 已确认输出一个主去向及该分支下的处理项列表，范围止于确定处理服务类型，不展开通知单生成、任务池操作及数据库更新。
- 人工处理建议的标记方式：必须区分代码退出事实与注释/日志说明，不声称已发生人工流转。
- 最小字段、缺失时顺序阻塞处理、结构化契约和选路验收标准。

本轮仅更新需求文档与术语，尚未修改应用代码。


### 本轮确认：覆盖范围与输出粒度

用户确认覆盖 dealIssue 全部选路分支及必要辅助判断，不包含 dealTransUW；输出一个主去向加该分支下处理项列表。组合处理可包含多项，不能强行压成单一动作。不包含服务执行细节。
下一轮待确认：精简记录与派生条件的职责边界；缺失前置条件时的选路阻塞行为。


### 本轮确认：确定性预计算与顺序阻塞

- 程序计算明确的条件，包括核保批次、记录存在性、历史匹配、外1历史不同批次下发次数等；AI 按原代码顺序选择主分支与处理项。
- 预计算必须保持原代码的统计范围与匹配口径，并保留输入记录引用；资料不足时为未知，不可当作 false。
- 前置条件未知且影响分支选择时，返回信息不足、无法确定及阻塞条件，不确定后续去向。
- 已知条件足以确定前置分支时，不要求补齐后续不影响该去向的资料。
- 实验验证模型执行确定性选路规则的能力，不代表重新进行医学或核保专业判断。以原代码预期去向为对照，评估 AI 是否值得保留。

后续待确认：手动输入到预计算的入口方式；结构化结果失败处理；验收与真实模型评测标准。此前要求模型完成全部简单统计的建议未被采纳，以本节为准。


### 本轮确认：输入、输出失败与验收

- 保留聊天框固定 JSON 输入；用户填写精简业务记录，后台计算条件。模型只接收选路必要条件、处理项及记录引用，不接收执行服务专用字段。
- 模型输出格式错误、非法去向或引用不存在的记录时，单独显示本次选路失败，不产生有效去向，不从自由文本猜测，不伪装成业务信息不足；首版不自动重试。
- 用户强调必须限制幻觉。结构和引用校验已确认；程序是否进一步校验分支条件、优先级和处理项完整性，下一轮明确。
- 程序预计算：人工确认案例覆盖批次、历史匹配、次数边界、未知状态，预期全部通过。
- 模型评测：固定模型配置及提示词，每例重复运行；分别统计主去向、处理项、引用正确性、格式失败率、耗时和调用成本（可获取时，不编造成本）。
- 首轮固定案例不得出现错误去向、遗漏处理项或越过未知前置条件选路；失败先定位，不宣称通过。合成案例不证明真实业务准确率。

仍待确认：运行时语义校验边界、首版 RAGFlow 是否参与、精简字段与输出契约终稿、重复次数。需求访谈继续，尚未修改应用代码。


### 本轮确认：运行时四层校验与 RAGFlow 边界

- 全部启用结构、引用、分支条件及优先级、处理项完整性四层校验。
- 程序保留核验所需规则；AI 的选路错误被拒绝，不自动改为程序计算的去向，不隐藏于评测指标。
- 前置条件未知时不得越过；处理项不得新增、遗漏或重复。校验不通过不产生有效去向。
- 首轮选路链路不接入 RAGFlow；原问答入口的 RAGFlow 保持现状。

## 本轮范围汇总（待整体核对）

手动精简记录 JSON -> 程序预计算三态条件和依据引用 -> AI 依固定提示词选路 -> 四层校验 -> 页面展示主去向、处理项、命中条件、输入依据、缺失信息。
覆盖 dealIssue 及必要辅助判断，不覆盖 dealTransUW，不执行任何业务接口。不恢复旧独立模块。单次完整输入独立判断，聊天历史不作为业务事实。
必须区分有效选路（含退出自动处理）、业务信息不足、模型调用或输出失败；退出不等于人工流转，建议通过不等于已核保通过。代码无明确后续动作时如实说明，不虚构服务。
原始资料只保留条件计算与依据定位必需字段；未知、未提供、确认无记录不能混淆。执行专用字段不交给模型。
程序规则与提示词需可对应同一判断规范，评测固定模型配置和提示词版本。语义校验不是模型准确性的证明，模型失败仍计入原始评测结果。
当前需求重新对齐尚未启动应用代码改造。细化字段及分支契约时依据 GBK AutoSendBL 原始代码，不要求业务人员猜数据库或计算条件。


## 2026-09-14：整体基线已确认，实施中

用户确认最终需求基线，并要求尽量复用现有代码。继续复用原接口、ChatRequest标记、页面开关/示例操作、SSE和消息展示；复用Jackson、Spring AI输出契约及既有模型配置。新增代码仅负责预计算、共享选路表和四层校验及隔离的单次模型调用。
操作、最小字段、分支映射和结构化输出说明见docs/trial/deal-issue-routing.md。35个代码对照案例及模型输出拦截、配置隔离测试已建立；真实服务尚未评测。


### 本轮工程验证完成

- 48项Java测试通过，其中36个固定代码分支对照案例、模型输出四层拦截、普通聊天接线与模型配置隔离测试。
- 6项页面交互检查通过，已检查桌面截图；继续复用原页面视觉样式。
- 旧模板及提示词已归档至docs/trial/archive，不再随应用发布。
- POM、认证账号配置、原问答RAGFlow及业务AutoSendBL.java未修改。
- 真实模型重复评测及业务负责人对案例的验收尚未完成，不宣称真实业务准确率达标。
- 本轮改动尚未提交或推送GitHub。


## 2026-09-15：用户确认使用框架结构化转换器

实际返回值含JSON代码围栏，严格readTree导致formatCorrect=false；去掉围栏后重放发现缺少completeness.lwmission，去向与路径本身正确。
用户授权复用BeanOutputConverter处理包装，传入严格ObjectMapper，再执行完整业务校验。包装清理不等于修补业务内容，不自动补引用、不改分支、不重试。
已接入框架Whitespace/Markdown清理器和convert方法，强制构造字段存在、拒绝多段及额外内容，细化页面失败原因。关闭转换器原文错误日志并移除已确认暴露密钥/解密正文的两处日志。
51项本地Java测试通过，含实际返回值重放、补全引用后的通过样例及真实日志配置测试。测试继续按用户要求保持忽略，未强制加入Git。真实模型重新评测尚未执行；本轮修复未提交推送。


## 2026-09-15：确认并实施模型只选分支（覆盖此前完整报告复制约定）

用户确认缩小模型输出，确定性内容归服务端；expected()仅作对照/拦截，不自动兜底。模型输入缩小为条件三态值；输出SELECTED+branchId或INSUFFICIENT+blockedAt。服务端在分支校验成功后重建路径、引用、缺失项和处理项；错误选择不产生报告，报告组装错误单独记录。保留严格结构转换和一次调用。

同时确认并实施：删除手填schemaVersion；当前uwno提到顶层，历史逐条保留；当前uwerror改成每条noFailedRules布尔值；历史不含uwerror。completeness仍保留，尚未授权切换完整性表达。前端示例校验、模板与36个固定案例同步迁移，既有案例预期不变。

补充决策表节点/引用/类型/条件覆盖/无环/可达性校验。60项本地Java测试通过。未对真实业务数据库执行SQL对拍，未进行真实模型质量验收。代码尚未提交推送。

本轮6项页面交互检查通过：示例填入、普通问答切换、取消保留输入、请求失败保留输入、窄屏布局及CDN不可用时填入示例。


## 2026-09-15：复用模型连接配置

核对实际JAR：DeepSeek 1.1.2无mutate/公开API getter，自动配置API方法私有。改用集中、只读的版本兼容适配复用原API，不从properties拼装；取消ObjectProvider依赖。标准模型以外的子类拒绝隐式重建。DashScope空options回退默认；两者显式NOOP模型观测，静态单次RetryTemplate。保留HTTP底层自有行为，单次约定仅覆盖模型重试层。

新增模拟HTTP测试验证自定义endpoint/path、header、拦截器、模型参数保留，原观测不变；补充DashScope空options及缺失DeepSeek API拒绝测试。测试无外部网络。代码尚未提交推送。


## 2026-09-15：输出校验及维护性收紧

用户指出围栏清理与提示词矛盾，改为只清理空白，围栏记格式失败，覆盖此前允许单围栏的约定。列表比较已在小结构改造中移除。新增InternalFailure封装对照/报告错误与cause，并按COMPARISON/REPORT审计，不归因于模型。

缓存提示词指纹；封装可变ObjectMapper，只提供副本和序列化；固定数据组/必填字段校验顺序，JSON解析错误保留cause；预计算集合常量与resolveSubject；入口来自决策表entry元数据，上限随nodes数量。保持全部原分支语义。新增针对性工程回归，未调用真实模型，尚未提交推送。


## 2026-09-15：SQL边界复现与修复

依据原码纠正审查假设：hasOther用于DISTINCT唯一值判定，不是SQL <>；空类型有体检项映射peitem有明确原码依据。已复现真实问题：NULL编码NOT IN空字典的组合候选被漏掉（72组SQLite独立预期中4组失败），expected缺失items返回null（1组失败）。修复SQL三值在WHERE边界转换，正向命名规则筛选；缺失items与未就绪叶子均内部失败。候选完整性UNKNOWN即返回UNKNOWN，Ready门槛继续保留。144项Java测试通过，实际业务数据库SQL对拍仍未执行。


## 2026-09-15：候选就绪与审计引用修复

删除hasNoteExam为FALSE时覆盖noteExamReady的逻辑，完整空列表保持Ready=TRUE。当前D09无候选本来就走NO_NOTE，之前没有复现报告失败，但Ready语义错误已修正。

hasCombined是原查询是否有候选，hasCombinedServices是是否有可执行服务；两者允许不同。D15经R_COMBO及D16，在没有服务时走NO_SERVICE，不补造映射，也不把查询改为排除原有类型。回归覆盖此路径。

NULL规则编码且字典非空时引用具体uwrulecode字段与整个字典（证明非空），不把第0项误写为匹配值。firstBatch同时保留当前行及顶层uwno。Ready的负向引用仍保留，使用appendAuditEvidence明确表示完整列表审计；exists命中后停止，其否定证据仍保留。输入拒绝字典空字符串，SQL NULL继续接受。

新增4项测试，修复前3项失败，修复后148项Java测试全部通过。此前5413549已通过GitHub API验证同步，仓库为私有。本节为后续补充修复；未修改用户新出现的test.py。


## 2026-09-15：RoutingInput契约与错误提示

保留completeness必填对象的现有行为，注释及文档明确{}和缺省/null标志可表示未知，对象缺失/null专用提示。标注ObjectMapper的树检查/POJO绑定职责，空输入与超长分开提示。contno业务格式尚未确认，仅保留试验标识非空文本与总输入上限。autoallotbyerr空字符串拒绝已在1acc274修复。本轮不修改用户test.py，尚未提交推送。


## 2026-09-15：RoutingPolicy遍历合并

修复测试构造器不完整初始化；expected改为纯ExpectedPath，validate比较后一次assemble，不再二次遍历。处理项异常REPORT、路径事实异常PATH_EVALUATION，INVALID是显式受约束的GUARD哨兵。增加安全原因枚举、显式Selection必填/互斥检查和display输入防御。提示词/指纹仍构造时缓存。158项Java测试通过，尚未提交推送。


## 2026-09-15：IssueSubmissionTrialService可观测性与超时

服务按阶段记录安全诊断，缺失metadata/usage不破坏选路，保留零token，空输出和输入拒绝分别审计。规则表改惰性加载，失败只影响实验请求；客户端隔离失败单独记录。响应等待时间通过issue-submission-trial.response-timeout-ms配置，默认50000，不承诺停止底层HTTP或计费。独立requestId关联超时、迟到模型用量和后台结束，无重试/兜底。费用提示集中常量、causeType判空。164项测试通过，包含迟到7token而无正常成功日志。尚未提交推送。


## 2026-09-15：代理适配与契约不可变性

实现可检查Spring AOP代理保留advisors及TargetSource生命周期，每次获取后隔离并释放，未透明剥离AOP；opaque/非标准目标明确拒绝。DeepSeek反射错误保留cause及reason，服务按隔离失败记录；共享工具设置解释职责。

records防御性复制集合与嵌套列表，Table使用List，Decision/ExpectedPath校验基本状态；完整报告保留显式route:null。Selection注解已在上轮移除。170项Java测试通过，尚未提交推送，test.py未修改。
